apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "cpp-make-none.fullname" . }}-test-connection"
  labels:
    {{- include "cpp-make-none.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "cpp-make-none.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
